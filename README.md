# Credit Rating Transition Simulation (Markov Chain)

A simple Markov-chain simulation of credit rating migrations:
- 1-year transition matrix visualization
- sample rating trajectories
- projected rating distribution over time
- cumulative default probability (PD)

## Quickstart

```bash
pip install -r requirements.txt
python src/simulate.py --seed 42 --start BBB
